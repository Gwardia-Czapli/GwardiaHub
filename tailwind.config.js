// Not used in code! Only for PyCharm to recognize Tailwind usage

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./**/*.html"],
  theme: {
    extend: {
      borderWidth: {
        '12': '12px',
      },
    },
  },
  plugins: [],
}

